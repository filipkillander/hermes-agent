# Knowledge root for #spel (fixture)

This directory is the read-only knowledge root surfaced by the #spel route-contract
for game-specific reference material. The contract tests verify that the route
resolves this root, applies a read-only policy, and never writes to it.

Do not modify live game data from this fixture directory.